{% extends "layout.twig" %}

{% block content %}
	{% for blogPost in blogPosts %}
		<div class="blog-post">
			<h2><a href="{{ 'detail/' | url }}{{ blogPost.blog_id }}">{{ blogPost.title }}</a></h2>
			<p>{{blogPost.created_at[0:10]}} por <a href="">{{ blogPost.author }}</a></p>
		</div>
		{% if blogPost.img_url %}
			<div class="blog-post-image img-fluid">
				<!--Ejemplo con imagenes random: -->
				<!--https://source.unsplash.com/random/800x600 -->
				<img src="{{ blogPost.img_url }}" alt="{{ blogPost.img_url }}">
			</div>
		{% endif %}
		<br>
		<div class="blog-post-content">
			<p>{{ blogPost.content[0:600] }}...</p>
		</div>
	{% endfor %}
{% endblock %}

{% block content2 %}
	<footer>&copy; Blogitare 2018. All rights reserved. <a href="{{'admin' | url }}">Admin Panel</a></footer>	
{% endblock %}
